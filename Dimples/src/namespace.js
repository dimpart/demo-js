'use strict';

//! require <dimsdk.js>
//! require <network.js>

var mk = DIMP;    // MONKEY
var mkm = DIMP;   // MingKeMing
var dkd = DIMP;   // DaoKeDao
var dimp = DIMP;
var sdk = DIMP;

var fsm = DIMP.fsm;     // FiniteStateMachine
var st = DIMP.network;  // StarTrek
var sg = DIMP.network;

// if (typeof DIM !== 'object') {
//     var DIM = {};
// }
// var dim = DIM;
var app = DIMP;
